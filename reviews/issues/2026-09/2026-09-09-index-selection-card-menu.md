# 선택 모드의 점 메뉴가 메뉴를 열지 않고 선택을 뒤집음

- ID: `REV-2026-056`
- 날짜: `2026-09-09`
- 보고자: `Codex`
- 상태: `resolved`
- 심각도: `P2`
- 영향 영역: `index`
- 관련 인계: `HANDOFF-2026-078`

## 요약과 영향

선택 모드의 카드 capture 클릭 처리기가 점 버튼의 클릭까지 가로채지만 점 버튼은 계속 보였다.

## 재현 절차

선택 모드에서 카드 오른쪽 점 버튼을 누른다. 메뉴가 열리지 않고 카드 선택 여부가 바뀐다.

## 기대 결과 / 실제 결과

기대는 조작 대상과 화면·저장 상태가 일치하는 것이다. 실제 불일치는 위 절차 및 `scripts/check-library-ui.mjs`에서 재현했다.

## 근거

기준 커밋 `96e9d83`의 `index.html`과 실제 Chromium 클릭 검사. UI 검사는 외부 통신을 차단한 별도 브라우저 저장소에서 수행한다.

## 처리 기록

- 2026-09-09 — Codex: 선택 모드에서는 카드 메뉴를 숨기고 일괄 작업 버튼을 사용한다. 전체 선택 상태에 맞춰 모두 해제 문구를 표시하고 화면 밖 선택 수도 알린다. 단일 카드에는 폴더 이동 항목을 추가하고 이동 목적지는 이름 목록에서 선택한다.
- 검증: `npm run test:library-ui`. 수정 전 실패와 수정 후 통과를 확인하며 기존 라이브러리 계약·브라우저 회귀도 함께 실행한다.
