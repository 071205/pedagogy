# 폴더 안에서 만든 새 문제집·A단계 복제본이 폴더 밖으로 빠짐

- ID: `REV-2026-054`
- 날짜: `2026-09-09`
- 보고자: `Codex`
- 상태: `resolved`
- 심각도: `P2`
- 영향 영역: `index`
- 관련 인계: `HANDOFF-2026-078`

## 요약과 영향

새 문제집 생성은 폴더 필터를 읽지 않았고, 복제는 sets만 복사하여 A단계의 folderBySetId를 복제하지 않았다.

## 재현 절차

수학 폴더를 연 뒤 새 문제집을 만들고 라이브러리로 돌아온다. 폴더에 보이지 않는다. A단계에서는 기존 카드 복제도 같은 현상이 난다.

## 기대 결과 / 실제 결과

기대는 조작 대상과 화면·저장 상태가 일치하는 것이다. 실제 불일치는 위 절차 및 `scripts/check-library-ui.mjs`에서 재현했다.

## 근거

기준 커밋 `96e9d83`의 `index.html`과 실제 Chromium 클릭 검사. UI 검사는 외부 통신을 차단한 별도 브라우저 저장소에서 수행한다.

## 처리 기록

- 2026-09-09 — Codex: 생성 시 현재 유효 폴더, 복제 시 원본의 유효 폴더를 setFolder로 지정한다. A단계 로컬 지도와 B단계 문서 필드를 각각 사용하고 로컬 메타도 저장한다.
- 검증: `npm run test:library-ui`. 수정 전 실패와 수정 후 통과를 확인하며 기존 라이브러리 계약·브라우저 회귀도 함께 실행한다.
