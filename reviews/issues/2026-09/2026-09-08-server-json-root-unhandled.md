# 배열 JSON을 로컬 변환 API에 보내면 연결이 예외로 끊긴다

- ID: `REV-2026-039`
- 날짜: `2026-09-08`
- 보고자: `Codex`
- 상태: `open`
- 심각도: `P2`
- 영향 영역: `server`
- 관련 인계: `HANDOFF-2026-063`
- 분석 기준: `cbd951e`의 격리 사본. 이후 `11ef093`의 변경 파일과 겹치지 않음.

## 요약과 영향

잘못된 가져오기/클라이언트 입력이 설명 가능한 400 대신 네트워크 장애로 표시된다. 서버 전체 프로세스가 종료되는 문제로 확인한 것은 아니다.

## 재현 절차

격리한 serve.py의 /render에 올바른 Host·X-Exam-Client 헤더와 JSON 본문 []를 한 번 전송한다.

## 기대 결과 / 실제 결과

- 기대: 공통 진입점에서 JSON 루트가 객체인지 확인한 뒤 경로별 스키마 검증을 수행한다.
- 실제: JSON 파싱은 성공하지만 req.get에서 배열에 없는 메서드를 호출해 처리 스레드가 예외로 종료된다. 브라우저 측 재현에서 UND_ERR_SOCKET을 확인했다.

## 근거

`serve.py:499–511` 및 _hwpx/_document_hwpx req.get 호출. 재현 도구: `reviews/audits/2026-09-08/audit-browser.mjs / MALFORMED_RENDER`.
인증·DB·AI·Storage 외부 경계는 모의 구현이며 운영 계정·사용자 데이터는 사용하지 않았다.
전체 범위와 제한은 [종합 보고서](../../audits/2026-09-08/REPORT.md)를 참조한다.

## 수정 설계

공통 진입점에서 JSON 루트가 객체인지 확인한 뒤 경로별 스키마 검증을 수행한다. 실패는 구조화된 400으로 반환하고 내부 경로나 stack은 노출하지 않는다.

## 완료 판단에 필요한 검사

null/배열/문자열/숫자를 세 POST 경로에 넣어 모두 400 확인. 그 직후 정상 요청이 성공해야 한다.

## 처리 기록

- 2026-09-08 — Codex: 실제 함수를 이용한 독립 재현으로 등록. 제품 수정은 하지 않았으며 Claude 검토 후 구현 예정.
