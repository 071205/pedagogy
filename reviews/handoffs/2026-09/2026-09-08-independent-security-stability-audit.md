# 변경 인계 — 독립 보안·안정성·효율성 종합 분석

후속: 사용자의 수정 승인으로 [HANDOFF-2026-067](2026-09-08-audit-findings-fixed.md)을 작성했다.
이 인계는 최초 감사 시점의 기록이다.

- ID: `HANDOFF-2026-066`
- 날짜: `2026-09-08`
- 작성자: `Codex`
- 상태: `ready-for-review`
- 영향 영역: `docs`, `worker`, `index`, `server`, `mock` (제품 코드 수정 없음)
- 관련 이슈: `REV-2026-031`~`REV-2026-039`

## 변경 내용

사용자 요청에 따라 이전 구현 리뷰와 별개로 처음 보는 관점에서 독립 감사했다.
[종합 보고서](../../audits/2026-09-08/REPORT.md)에 관점별 체크리스트, 실제 재현 결함 9건,
검증하지 못한 운영 경계, 수정 설계와 순서를 남겼다.
[실행 증거](../../audits/2026-09-08/RESULTS.md) 및 독립 재현 도구 4개를 함께 제공한다.

## 위험과 검토 요청

먼저 계정 전환의 늦은 응답(032), 비용 카운터 초기화(031), 저장 ack의 잘못된 버전(033),
로컬 저장 실패 상태(038)를 검토해 달라. 기능 정상 동작·토큰 서명·Rules 소유자 제한이
있어도 상태 전환의 보장은 별도라는 점이 핵심이다.

이미지 원본 수명(034), 자기 데이터 정규화 손실(036), HWPX 전역 상태 경쟁(035)은
개별 패치 이전에 복구 정책과 상태 모델의 합의가 필요하다. 브라우저 이식에 서버의 전역
가변 상태를 그대로 옮기지 않도록 특히 035를 함께 확인해 달라.

## 검증

- 기준 커밋 cbd951e를 임시 디렉터리로 격리. 최신 ae5740e에서도 재현 도구 4개를
  재실행해 9건 모두 여전히 재현됨을 확인했다. Claude 제품 작업 파일은 수정하지 않았다.
- 최초 브라우저 회귀 128/128, Worker·정적 검사 통과. 최신 브라우저 회귀 136/136 통과.
- 도중 추가된 브라우저 emitter/ZIP 전체 구현은 이번 감사 범위 밖이다. 035의 입증 범위는
  Python 서버 변환 경로이며 브라우저 엔진도 동일 결함이라고 단정하지 않는다.
- 실제 함수·로컬 HTTP·실제 HWPX build로 결함 재현. 인증/AI/DB/Storage 외부 경계는 fixture.
- 로컬 RSA 서명 및 변조 거절 확인. npm audit 0건. 출시 gate는 설정 공란 등 6개로 실패.
- 운영 환경·실사용 계정·Rules 에뮬레이터·실제 한글 시각 출력은 이번에 검증하지 않았다.

## 다음 검토자에게

현재 제품 코드는 고치지 않았다. 각 이슈의 재현을 확인한 뒤 설계에 피드백을 남겨 달라.
재현 스크립트의 성공은 **취약 동작이 재현됐다는 의미**인 항목이 있으므로 CI에 바로 연결하지
말고, 수정 단계에서 안전한 기대 결과로 바꿔 사용한다. 해결 기록은 같은 이슈 파일에 남긴다.
