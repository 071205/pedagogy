# 변경 인계 — DEV-5 개발 토큰 효과 확인

- ID: `HANDOFF-2026-132`
- 날짜: `2026-09-13`
- 작성자: `Codex`
- 상태: `reviewed`
- 영향 영역: `docs`
- 관련 이슈: `없음` (열린 `REV-2026-074`, `REV-2026-075` 유지)

## 결과

[DEV-5 효과 확인](../../../docs/DEV-TOKEN-EFFECT-CHECK.md)을 완료했다. DEV-1의 고정 질문 두 개를
추출 전 `bc9c6f9^`와 추출 후 `bc9c6f9`에서 다시 찾았다.

사진 AI 전체 흐름은 211줄/1파일에서 본체 177줄 + 새 모듈 57줄, 합계 234줄/2파일이 됐다.
AI 전처리·응답 변환만 찾을 때는 새 57줄 파일로 바로 좁혀지지만, 동의·인증·요청·응답 반영까지
포함한 유지보수에는 읽을 범위와 파일 왕복이 줄지 않았다. 미리보기 확대 경로는 변경되지 않았다.

새 module·공개 빌드·로컬 서버 허용 목록·전용 133줄 브라우저 검사로 재발 방지 경계는 강해졌지만,
이는 이번 변경의 검증 부담도 늘린다. 실제 후속 AI 변환 수정 사례가 없으므로 장기 절감은 추정하지 않는다.

## 결정

추가 프런트 분리는 보류한다. 이 첫 경계는 안전한 구조 사례로 유지하되, 개발 토큰을 더 줄인다는
근거만으로 B·C를 분리하지 않는다. DEV-5는 완료했고 다음 단계는 OPS-6/Terra medium 서비스 측정
배포 준비다. Worker quota·max_tokens·공급자·프런트 동작·배포·실제 API 호출은 바꾸지 않았다.

## 검증

- `git grep`과 함수 경계로 추출 전후 두 질문의 진입 위치·범위·파일 수를 대조했다.
- `wc -l -c`로 index와 새 모듈의 실제 크기를 확인했다.
- `git diff --shortstat bc9c6f9^ bc9c6f9`로 구현·검사 추가 부담을 확인했다.
- 문서만 변경했으며 제품 회귀는 DEV-3/4 통과 결과를 재실행으로 대체하지 않았다.

## 다음 작업자에게

OPS-6/Terra medium은 운영 상태를 바꾸지 않고 실제 배포본과 로컬 Worker 차이, 대상 환경, bindings,
기존 검사·롤백 버전, 합성 검증의 최대 호출·비용 상한을 읽기 전용으로 정리한다. 대상·예산·실호출은
사용자 승인 전까지 만들거나 실행하지 않는다. `transcript.txt`는 사용자 미추적 파일로 유지한다.
