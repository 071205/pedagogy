# 변경 인계 — AI 비용 개선 재검토와 모델 전환 진행 기준

- ID: `HANDOFF-2026-126`
- 날짜: `2026-09-13`
- 작성자: `Codex`
- 상태: `ready-for-review`
- 영향 영역: `docs`, `tests` (리뷰 진단 도구만)
- 관련 이슈: `REV-2026-090` 신규 open, `REV-2026-089` 해결 재확인

## 변경 내용

사용자는 Astra high로 현재 변경을 다시 검토하고, 모델만 바꿔도 다음 작업을 이어갈 파일을 요청했다.
[진행 기준](../../../docs/AI-COST-ROADMAP.md)에 현재 위치 A, 단계별 모델·입력·완료 기준·진행표와
외부 배포/비용 승인 경계를 확정했다. AGENTS와 INDEX에 진입 링크를 남긴다.
제품 코드·기존 회귀 검사·설정은 변경하지 않았다. 구현 작업 A는 다음 모델의 범위다.

## 검토 결과

- eb120d2의 토큰 누락 수정 유효: 문항/문서 각 10종 가짜 응답에서 task/outcome·토큰/중단 사유 보존,
  이벤트 1회, reserve→consume, 가짜 원문 비노출을 확인했다. 089 resolved 유지.
- 125 검증의 범위 부족: 문서 프롬프트를 실제 console.error에 넣는 메모리 코드 변이가 현재 계약
  검사를 통과한다. 090으로 등록했다. 이미지 누출 변이는 단언으로 실패하므로 포착 자체는 작동한다.
- 이전 답변의 “프롬프트 포함 전체 console 비노출 검사·실제 실패 주입 완료”는 과장이다.
  제품 개인정보 유출이 확인된 것은 아니며 검사 보강 A와 독립 검토 B 뒤 배포 준비로 진행한다.
- 성공 30~50건만으로 p95/실패율의 신뢰도를 보장하지 않으며 10% 표본을 전체 과금으로 환산하지 않는다.
  2번은 근거에 따라 상한 유지로 끝낼 수 있고, 공급자 비교/어댑터/프런트 분리는 조건부다.

## 검증

- `node reviews/audits/2026-09-13/worker-telemetry-review.mjs`: 20개 경계 PASS, baseline 0,
  document-prompt-leak 0(MISSED), image-input-leak 1(AssertionError). 도구 종료 1은 090의 재현 결과다.
- 도구는 소스 파일을 쓰지 않고 자식 프로세스 출력에서 원문을 노출하지 않는다. 실제 fetch는 가짜다.
- 현재 제품 불변으로 기존 통과 Worker 전체 묶음은 반복하지 않았다. 이번 진단에서는 현재 요청 계약
  baseline과 두 변이를 실행했다. 문서 링크와 diff 공백은 별도 확인한다.
- 배포·운영 조회·실제 Anthropic 호출은 없다. 기존 열린 074/075는 이번 범위 밖으로 유지한다.

## 다음 검토자에게

진행 기준 A의 검사 보강으로 시작한다. 설계를 처음부터 다시 묻지 말고 해당 범위와 090만 구현한다.
이 인계 완료는 레일 설계 완료이며 로그 검증 A 또는 실환경 단계 완료를 뜻하지 않는다.

## A 완료 기록 (2026-09-13 · Codex)

`HANDOFF-2026-127`에서 090을 해결했다. 기준 응답 경계 20개 통과, 기본 Worker console 경계의
문항·문서 원문 비노출 단언, logger 실패 안전성과 네 실제 소스 변이의 AssertionError 실패를
`test:worker`에 연결했다. 다음 위치는 B 독립 검토다.
