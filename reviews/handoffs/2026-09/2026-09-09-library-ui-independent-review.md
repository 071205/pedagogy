# 검토 인계 — 라이브러리 사용 흐름(HANDOFF-2026-078) 실제 화면 독립 검증

- ID: `HANDOFF-2026-080`
- 날짜: `2026-09-09`
- 작성자: `Claude`
- 상태: `ready-for-review`
- 영향 영역: `docs` (제품 코드 변경 없음)
- 검토 대상: `HANDOFF-2026-078` / `REV-2026-053` ~ `-058`
- 기준 HEAD: `1879b90`

## 왜 실제 화면으로 했나

`-078` 의 6건은 **사용자 조작 흐름**이다 — "숨은 동작을 찾을 수 있는가", "이동 직후
⌘Z 가 되는가" 같은 것은 코드만 읽어서는 확인했다고 말할 수 없다. `test:library-ui`
11 시나리오가 `check:fast` 에 걸려 있고 그것도 통과하지만, **검사가 보는 것과 사람이
보는 것이 같은지**를 한 번은 눈으로 맞춰야 한다.

`serve.py --port 18992` 로 띄워 브라우저에서 직접 조작했다. **평소 쓰는 8787 과 다른
포트**를 써서 사용자의 로컬 데이터(localStorage 는 포트까지 포함해 갈린다)는 건드리지
않았다.

## 결과 — 6건 전부 실제 화면에서 확인. 결함 없음

| 이슈 | 확인한 것 |
|---|---|
| `REV-2026-053` | 폴더 칩 옆 `⋮`(접근성 이름 `고1 공통수학 폴더 관리`) → **폴더 관리** 대화상자에 이름 변경 + **명시적 `폴더 삭제`** 버튼. **"폴더를 삭제해도 문제집은 남고 '폴더 없음'으로 이동합니다"** 안내까지 있다. 우클릭·이름 비우기 같은 숨은 동작이 아니다 |
| `REV-2026-054` | 폴더를 만들면 **바로 그 폴더로 이동**하고, 그 안에서 만든 새 문제집이 **폴더에 남는다**(`고1 공통수학 1` · `폴더 없음 1`) |
| `REV-2026-055` | 검색이 폴더 필터를 넘어 **전체를 대상**으로 하고, 결과 카드에 **소속(`고1 공통수학`)이 표시**된다. 검색 중 폴더 칩을 누르면 **검색이 지워지고** 그 폴더로 이동한다 |
| `REV-2026-056` | 선택 모드에서 카드 `⋮` 가 **사라지고** 체크박스로 바뀐다(선택이 뒤집히는 혼란 없음). `0개 선택` → `1개 선택`, 버튼 문구가 `모두 선택` ↔ `모두 해제` 로 따라간다 |
| `REV-2026-057` | **가장 중요한 확인.** `currentSetId`=`가`(`q92oe4vh`) 인 상태에서 라이브러리의 **`나`** 이름을 바꾸니, `qg7v8pgj` 시각만 `…069887 → …113487` 로 갱신되고 **`가` 는 `…045124` 그대로**였다. `최근 수정순` 정렬에서 **`나2` 가 위**로 올라온다 — 편집 중이던 `가` 가 아니다 |
| `REV-2026-058` | 카드 메뉴 `폴더로 이동` 이 **번호 입력 prompt 가 아니라 폴더 이름 선택 상자**다. `폴더 없음` 으로 옮긴 뒤 **⌘Z 한 번**에 되돌아왔다(`고1 공통수학 1` 복원) |

덧붙여 정렬 메뉴가 현재 기준을 `✓` 로 표시하고 **Escape 로 닫히며**, `manual` 이
`기본 순서` 로 보이는 것도 확인했다.

## 관찰 — 이슈로 올리지 않은 것

**라이브러리 화면인데 상단 바에 편집기 도구가 남는다** — `상단 문구(좌상단 인쇄용)` ·
`AI 프롬프트` · `인쇄` · `내보내기` · `저장` · `불러오기`. 편집 중인 문제집이 있을 때만
그렇고, 갓 부팅해 아무 문제집도 열지 않았으면 깨끗하다.

라이브러리에서 `인쇄` 를 누르면 `buildPrintDoc()` 이 `activeSet()` 을 쓰므로 **마지막에
열었던 문제집**이 인쇄된다 — 파괴적이지는 않지만 화면과 동작이 어긋난다.

⚠️ **오늘 작업의 회귀가 아니다.** `e11b1b1`(오늘 커밋 전)로 worktree 를 따로 띄워
같은 조작을 재현했고 **두 커밋의 결과가 완전히 같았다**:

```
e11b1b1 | 첫 라이브러리: [상단 문구, AI 프롬프트, 인쇄, 내보내기, 저장, 불러오기]
HEAD    | 첫 라이브러리: [상단 문구, AI 프롬프트, 인쇄, 내보내기, 저장, 불러오기]
```

그래서 `-078` 의 결함으로 등록하지 않았다. 고칠 값이 있다고 보면 **별도 이슈**로 열
문제이고, 판단(의도된 '빠른 인쇄' 인지 아닌지)이 먼저 필요하다.

## 검증 환경

- `serve.py --port 18992`, Chromium. 로그인하지 않은 상태(A단계 · `libraryCloudSchema: 0`)
  이므로 소속은 계정별 로컬 metadata 에만 쓰인다 — 운영 Firebase 에는 아무것도 쓰지 않았다.
- 비교용 worktree(`e11b1b1`)와 두 서버는 확인 뒤 정리했다.
- `npm run check:fast` 종료코드 0 · 회귀 154/154(별도 실행).

## 다음 검토자에게

`-078` 에 대해 재현된 결함은 없다. 위 '관찰' 하나를 어떻게 볼지만 판단해 달라.
