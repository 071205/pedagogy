# U0 연결 시안 — 사용 과제·관찰표·결정표

- 작성: 2026-09-23 · Claude / Opus 5 (⑥-A 두 번째 지시문)
- 시안 파일: [`mockups/u0-flow.html`](mockups/u0-flow.html) — **모의 데이터**. 앱 코드가 아니다.
- 계약 원문: [U0 계약](PRODUCT-UX-U0-CONTRACT.md) · 요구: [제품 UX](PRODUCT-UX-DESIGN.md) · [저장 계약](STORAGE-CONTRACT.md)
- 검토 판정과 발견은 [HANDOFF-167](../reviews/handoffs/2026-09/2026-09-23-docs-u0-contract.md)에 있다.

⚠️ **이 문서는 사용성 검증 결과가 아니다.** 관찰값 칸은 **비워 두었다** —
실제 사용자가 수행하기 전에는 채우지 않는다. 시안을 그렸다는 것이 확인했다는 뜻이 아니다.

⚠️ **D1은 2026-09-23 사용자가 독립/수정 연동을 선택 가능하게 하라고 결정했다.** 기존 시안은
독립 사본 가정으로 그린 과거 안이며, 화면 4·6의 선택/전파 표시를 보완해야 한다.
**D2/D4는 미결**이다. 종료 시 원문 삭제·문항 편집 우선은 시안의 권장 가정이다.

---

## 1. 시안이 잇는 흐름

```
라이브러리 ─┬─ 직접 만들기 ──────────────────────────→ 문항 편집 ⇄ 지면 배치
            └─ 자료로 만들기 · AI
                 → 자료+요청 → 쪽별 처리 ─┬─ 전부 성공 ─→ 구성안 → 권별 결과 ─→ 문항 편집
                                          └─ 부분 실패 ─→ 성공분만 사용 / 실패 쪽 재시도
                                                            ↘ 재개 · 원문 만료
                                                               문항 편집 → 충돌(B4) → 복구
```

시안 화면 아홉은 위 상자와 1:1이다. 실패 경로가 **막다른 길로 끝나지 않고** 늘
`구성안 → 권별 결과 → 편집`의 정상 경로로 돌아온다 — 그것이 '연결'의 내용이다.

### 화면마다 무엇을 지키는가

| # | 화면 | 지키는 계약 |
|---|---|---|
| 1 | 라이브러리·시작 | 두 경로 동시 노출 · JSON 불러오기와 PDF 투입 분리 · 영구 자료함 안 만듦 |
| 2 | 자료+요청 | 자료와 요청 한 화면 · 파일 선택만으로 과금 없음 · 추출 전 문항 수 미확정 |
| 3 | 처리·부분 실패 | 쪽만 숫자로 셈 · 결과를 드러내는 버튼 이름 · 자동 유료 재시도 없음 |
| 4 | 구성안+원문 | 손수 고친 구성이 기준 · 미분류 명시 처리 · **공통지문 경고**(아래 §3) |
| 5 | 권별 저장 결과 | '5권 완료' 금지 · 로컬/ACK 구분 · 크기 초과는 그 권만 · 실패 권 초안 보존 |
| 6 | 문항 편집 | 기존 `#paneTabs`·`#spanSeg` 재사용 · 미검토/확인함 구분 |
| 7 | 전체 지면+경고 | PDF 없이 잘림 확인 · 맥락별 설정 · 경고에서 위치로 이동 · 기존 출력 경로 공유 |
| 8 | 충돌 복구 | 네 선택지 지속 표시 · 두 revision · 시각은 설명용 · 사본 1회 |
| 9 | 재개·원문 만료 | 같은 채택 ID · 삭제 권 자동 부활 금지 · 성공 잠금 중 자동 재호출 금지 |

---

## 2. 여섯 사용 과제 — 시나리오와 관찰표

제품 UX §7의 여섯 과제다. **세 화면 크기에서 각각** 수행한다.
`1194×834`(아이패드 가로/노트북) · `768×1024`(태블릿 세로) · `375×812`(휴대폰).

각 과제는 **첫 클릭 · 완료 여부 · 잘못 누른 횟수 · 화면 왕복 · 재출력 횟수 · 미결**을 적는다.
⚠️ 관찰 전에는 **비워 둔다.** 추정으로 채우면 이 표가 거짓말이 된다.

### 과제 ① 시작 발견 — 설명 없이 두 경로를 찾는가

> "PDF 한 묶음이 있습니다. 이걸로 문제집 5권을 만들어 보세요."
> 그리고 "이번엔 AI 없이 빈 문제집을 직접 만들어 보세요."

- 시안 화면: 1
- 보는 것: '+ 새 문제집'을 누르는가, 아니면 '문제집 파일 불러오기'를 먼저 누르는가.
- 실패 신호: JSON 불러오기를 PDF 투입으로 착각한다 / 빈 라이브러리에서 무엇을 할지 모른다.

| 화면 | 첫 클릭 | 완료 | 오조작 | 왕복 | 미결 |
|---|---|---|---|---|---|
| 1194×834 | | | | | |
| 768×1024 | | | | | |
| 375×812 | | | | | |

### 과제 ② 이동 / Undo / 원문 복귀

> "3번 문항이 엉뚱한 권에 들어갔습니다. 옮기고, 취소하고, 원문이 몇 쪽이었는지 확인하세요."

- 시안 화면: 4
- 보는 것: 이동 후 Undo 버튼(`문항 3개 이동 취소`)을 찾는가 · 원문 상세를 여는 경로를 찾는가.
- ⚠️ **375에서는 상세 단이 접힌다** — 상단 `상세` 단추로 열고 `← 권 목록`으로 돌아오는지 본다.
- 실패 신호: 브라우저 뒤로 가기를 Undo로 쓴다 / 되돌리려고 수동으로 다시 옮긴다.

| 화면 | 첫 클릭 | 완료 | 오조작 | 왕복 | 미결 |
|---|---|---|---|---|---|
| 1194×834 | | | | | |
| 768×1024 | | | | | |
| 375×812 | | | | | |

### 과제 ③ PDF 없이 지면 조정

> "12번이 잘린다고 합니다. **PDF를 만들지 말고** 확인해서 고치세요."

- 시안 화면: 7
- 보는 것: '확인할 문항 3개'에서 `이동`을 눌러 그 위치로 가는가 · '한 쪽'으로 바꾸고 결과를 같은 화면에서 확인하는가.
- **재출력 횟수가 이 과제의 핵심 지표다** — 목표는 0이다. 지금은 인쇄 미리보기를 띄워야만 안다.
- 실패 신호: 확인하려고 인쇄 대화상자를 연다 / 88% 축소를 눈치채지 못한다.

| 화면 | 첫 클릭 | 완료 | 오조작 | 왕복 | **재출력** | 미결 |
|---|---|---|---|---|---|---|
| 1194×834 | | | | | | |
| 768×1024 | | | | | | |
| 375×812 | | | | | | |

### 과제 ④ 같은 문항으로 복귀

> "12번 지면 배치를 바꾼 뒤, 그 문항의 본문을 고치고, 다시 지면으로 돌아오세요."

- 시안 화면: 7 → 6 → 7
- 보는 것: 전환 뒤 **선택과 스크롤 위치**가 이어지는가 · 12번을 다시 찾느라 목록을 훑는가.
- 실패 신호: 전환 후 1번으로 돌아가 있다 / 다른 앱으로 왔다고 느껴 뒤로 가기를 누른다.

| 화면 | 첫 클릭 | 완료 | 오조작 | **왕복** | 미결 |
|---|---|---|---|---|---|
| 1194×834 | | | | | |
| 768×1024 | | | | | |
| 375×812 | | | | | |

### 과제 ⑤ 다섯 가지 상태를 구별하는가

> "방금 5권을 만들었습니다. **어느 권이 안전하고 어느 권이 위험한지** 말해 주세요."

- 시안 화면: 5 (+3의 실패 쪽, 4의 미검토)
- 다섯을 구별해야 한다 — **실패 쪽 · 미분류 · 미검토 · 이 브라우저에만 · 클라우드 동기화됨**.
- ⚠️ **색만 보고 답하는지 본다.** 색각 이상·흑백 인쇄에서도 글자로 읽혀야 한다.
- 실패 신호: "5권 다 됐네요"라고 답한다 / '저장됨'과 '동기화됨'을 같은 말로 읽는다 /
  '미검토'를 '오류'로 읽는다.

| 화면 | 다섯 중 맞힌 수 | 색만 의존 | 오조작 | 미결 |
|---|---|---|---|---|
| 1194×834 | /5 | | | |
| 768×1024 | /5 | | | |
| 375×812 | /5 | | | |

### 과제 ⑥ 세 크기에서 핵심 기능과 복귀 동선

> 위 다섯 과제를 **375에서 그대로** 해 보세요.

- 보는 것: 접힌 단을 여는 단추를 찾는가 · **`←` 복귀가 데이터를 버리지 않는다고 믿는가** ·
  터치 대상이 충분한가(44px) · 키보드만으로 도달 가능한가.
- ⚠️ 이 저장소의 계약: **hover 전용·드래그 전용 기능을 만들지 않는다.**
  시안의 모든 조작은 단추다 — 드래그로만 되는 이동이 없다.
- 실패 신호: 375에서 구성안의 권을 바꾸지 못한다 / `←`를 눌렀다가 초안이 사라질까 봐 멈춘다.

| 항목 | 1194×834 | 768×1024 | 375×812 |
|---|---|---|---|
| 핵심 기능 도달 | | | |
| 복귀 동선 이해 | | | |
| 터치 대상 44px | | | |
| 키보드 전용 통과 | | | |

⚠️ **Playwright WebKit은 실제 iOS 사파리가 아니다.** 375 결과를 자동 검사로 채우지 않는다 —
`test:cross`가 초록불이어도 '모바일 확인됨'이라고 쓰지 않는다(CLAUDE.md).

---

## 3. 시안이 계약에 **더한** 것 — 공통지문 경고

U0 계약 §3은 "초기안은 공통지문 묶음 단위 이동·채택"만 적는다.
독립 검토에서 **계약에 없는 실패 방향**을 재현했다(`REV-2026-108`).

| 사용자가 하는 일 | 지금 코드가 하는 일 |
|---|---|
| 묶음 가운데 문항을 **제외** | `groupSpan`이 그대로라 **뒤의 무관한 문항이 묶음에 흡수**된다 |
| 지문 보유 문항만 **이동** | 목적지의 다음 문항들이 그 지문 범위로 **잘못 묶인다** |
| 지문 없는 뒤 문항만 **이동** | 지문이 **조용히 사라진다**(경고 없음) |

셋 다 오류가 나지 않고 **인쇄물만 틀린다.** 그래서 시안 화면 4는 묶음을
`grouped` 세로선으로 묶어 보이게 하고, 선택 시 **무엇이 깨지는지 미리 말한다**:

> 02–04는 지문 하나를 함께 씁니다. 따로 옮기면 03·04에는 지문이 따라가지 않고,
> 02만 옮기면 목적지의 다음 두 문항이 이 지문 묶음으로 잘못 묶입니다.
> `[세 문항을 묶음째 옮기기]` `[묶음 풀고 지문 복제하기]`

▶ **계약 보강 제안**: U0 §3의 지문 항목에 **목적지 쪽 흡수**를 추가하고,
채택 시점에 `groupSpan`을 **실제 배치된 연속 구성원 수로 다시 쓴다**고 명시할 것.
D1(독립 사본)에서는 '묶음 풀고 지문 복제'가 자연스러운 선택지가 된다 —
**풀/혼합이면 지문을 공유 참조로 둘지 다시 정해야 한다.**

---

## 4. D1 · D2 · D4 결정표

⚠️ **D1 방향은 사용자 결정이다. D2/D4는 미결이다.** 아래 '시안 가정'은 그림을 그리기
위한 전제일 뿐 구현 승인이 아니다.

| ID | 질문 | 선택지 | 시안이 쓴 가정 | 고르면 바뀌는 화면 | 막고 있는 것 |
|---|---|---|---|---|---|
| **D1 — 방향 선택** | 문항을 다른 권에 넣을 때 독립/수정 연동을 고르게 | **사용자 결정: 두 방식 선택 가능** | 기존 독립 사본 시안 | 화면 4의 삽입 방식 선택, 화면 6의 영향받는 권/여기서만 고치기 표시와 연동 해제 흐름이 필요하다. 지문 참조·원본 삭제는 추가 계약 대기 | B3 현재 문제집 CAS는 독립 전환 가능. B4/B6의 연동 저장 구조는 Astra high 설계 전 대기 |
| **D2** | 원문 PDF를 언제까지 두나 | ① 작업 중만, 종료 시 삭제 ② 결과 생성 뒤에도 일정 기간 보존 | **① 종료 시 삭제** | ②면 화면 9의 '원문 지워짐'이 '원문 n일 남음'이 되고, 기간 표시·수동 삭제 UI가 화면 1이나 설정에 새로 필요하다 | B6/B7 원문 수명 |
| **D4** | 결과를 만든 뒤 어디로 보내나 | ① 문항 편집 우선 ② 지면 우선 / 한 권이면 편집 직행 ③ 항상 결과 목록 | **①+②혼합**: 한 권이면 편집 직행, 여러 권이면 결과 목록 | ②(지면 우선)면 화면 5→7로 잇고 6은 두 번째가 된다. ③이면 한 권일 때도 목록을 거친다 | U1/U2 화면 확정 |

### D4 비교안 — 만든 직후 어디로 가는가

| | ⓐ 편집 우선 (시안) | ⓑ 지면 우선 | ⓒ 항상 결과 목록 |
|---|---|---|---|
| 한 권일 때 | 편집기로 직행 | 지면 보기로 직행 | 목록 한 번 거침 |
| 여러 권일 때 | 결과 목록 → 권마다 `편집하기` | 결과 목록 → 권마다 `지면 보기` | 같음 |
| 잘 맞는 상황 | **막 뽑은 문항은 미검토다** — 먼저 내용을 본다 | 인쇄가 목적이고 내용은 이미 믿을 때 | 저장 결과를 늘 확인시키고 싶을 때 |
| 위험 | 지면 잘림을 늦게 안다 | **미검토 문항을 안 보고 인쇄로 간다** | 한 권일 때 클릭이 는다 |
| 과제 영향 | ③·④가 쉬워짐 | ③이 쉬워지고 ⑤가 어려워짐 | ⑤가 쉬워지고 ①이 어려워짐 |

▶ **권장은 ⓐ다.** 이유는 취향이 아니라 계약이다 — 갓 만든 문항은 전부
`미검토`이고, 계약이 "저장했다고 검토 완료로 바꾸지 않는다"를 명시한다.
**검토하지 않은 것을 먼저 인쇄로 보내는 기본값은 그 계약과 어긋난다.**

### 아직 답을 만들 수 없는 것

| | 왜 |
|---|---|
| **D3** 비용·한도 숫자 | C2 실측 대기. 시안은 `C2 측정 대기` 표시만 두고 숫자를 쓰지 않았다 |
| **D5** 배포·실호출 승인 | 사용자 판단. 시안은 이를 전제하지 않는다 |
| 색·간격·밀도 확정 | 시안은 기존 토큰(`--blue #3182F6` 등)을 그대로 썼다. 시각 디자인 변경은 별도 비교가 먼저다 |

---

## 5. 시안이 일부러 하지 않은 것

- **여백 mm 자유 조절** — 제품 §4가 "별도 요구 확정 전 추가하지 않는다"고 했다.
  기존 `묶기 / 한 단 / 한 쪽` 세 배치만 썼고 `SPAN_HINT` 문구도 그대로 옮겼다.
- **별도 미리보기 조판기** — 화면 7의 지면은 `buildPrintDoc`/`fitPrintDoc` 경로를 **쓴다는 전제**로
  그렸다. 시안 안의 종이 그림은 배치 설명용이지 새 조판기 제안이 아니다.
- **AI 대화 탭** — 작업물이 주인공이라는 원칙(제품 §3-1)에 따라 채팅 UI를 만들지 않았다.
- **드래그 전용 이동** — 375에서 죽는다. 모든 이동은 단추로도 된다.
- **퍼센트 진행 막대** — 쪽은 셀 수 있으므로 `41/44`로 쓰고, 셀 수 없는 것은 숫자를 지어내지 않았다.
- **모의고사 편집기 통합** — 제품 §8이 "현재 별도 형식이고 자동 변환이 된 것처럼 표시하지 않는다"고 했다.

## 6. 다음

1. **D1 선택을 반영할 연동 저장 계약**을 `Astra high`로 설계·검토한다. D2/D4는 사용자가 고른다(§4 표).
2. D1의 새 연동 경계는 기존 B3 문제집 CAS와 구별하고 B4/B6 구현 전에 확정한다.
3. 여섯 과제를 **실제로 수행**하고 §2 표를 채운다. 그 전에는 검증 완료가 아니다.
4. U1이 화면 6·7을, U2가 화면 2·3·4·5를 구현한다. 선행은 `RAIL-ORDERS.md`를 따른다.
5. `REV-2026-108`은 U0 구현과 **독립으로** 지금 제품에서도 재현된다 — 수정 묶음을 따로 잡는다.
